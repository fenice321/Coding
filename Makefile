#目标对象:依赖对象
#水平制表符('\t')执行指令
#解释:
#目标对象:是要生成的可执行程序的名称
#依赖对象:是生成目标对象所需要的源码文件
#make在解释执行的时候会找到当前所在目录中的Makefile文件,在这个文件中找到第一个目标对象执行这个目标对象对
#应的指令
src=$(wildcard ./*.c)
obj=$(patsubst %.c, %.o, $(src))
main:$(obj)
	gcc $^ -o $@
%.o:%.c
	gcc -c $< -o $@
#makefile中的清理动作,这个对象是第二个目标对象,不会自动执行,只会指##定执行
#声明伪对象,伪对象不管如何每次都要重新生成
.PHONY:clean
clean:
	rm -rf main $(obj)

